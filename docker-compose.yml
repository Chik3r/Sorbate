services:
  sorbate.discordbot:
    image: sorbate.discordbot
    build:
      context: .
      dockerfile: Sorbate.DiscordBot/Dockerfile
    volumes:
      - app-data:/app
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: L6NFJZ#KmwUkFm

volumes:
  app-data:
  sorbate:
    image: sorbate
    build:
      context: .
      dockerfile: Sorbate/Dockerfile
